/* Alias solicitado por la especificación (javascript.js en la raíz).
   El proyecto separa el JS real en dos módulos, ya enlazados en cada
   página HTML:
     - js/nav.js         → navegación, migas de pan, progreso, glosario
     - js/activities.js  → motor de actividades interactivas y feedback
   Este archivo se deja como referencia/alias para quien busque un único
   "javascript.js" en la raíz, tal como se nombra en la estructura interna
   de eXeLearning. No es necesario incluirlo en las páginas: nav.js y
   activities.js ya cubren toda la funcionalidad. */
